# Android-Studio---Task-5
<p>Change the book filter application from day 4 to work with device onboard SQL.
The application must do GET query once, write all book data to local Room/SQL storage, and then operate with the local SQL storage only.</p>
